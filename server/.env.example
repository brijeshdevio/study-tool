# Environment Variables for Vercel Deployment
# Copy this to .env for local development
# Add these variables to your Vercel project settings

# CORS Origins (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com,https://yourapp.vercel.app

# Port (Vercel will handle this automatically, but useful for local dev)
PORT=3000

# Add any other environment variables your app needs here
